import { Point } from '../entity/point';

export class MagnestismServiceStub {
    isActivated = false;
    corner = 'Top Left';
    getClosestPoint(point: Point) {/** */}
    getClosestValue(value: number, incrementValue: number) {/** */}
}
