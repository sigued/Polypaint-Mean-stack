// import { Actions } from './action';

describe('Action Entity', () => {
    // let action: Actions;

    it('should return opacity 0', () => {
       /*  Actions singleton = MySingleton.getInstance();
        assertThat(singleton.getSomething(), is("something")); */
    });

    it('should return value white', () => {
      // expect(color.value).toEqual('white');
    });

  });
